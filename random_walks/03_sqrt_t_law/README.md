# 03 — The √t Law

Pin down the headline result: the std of position at time *t* is exactly √t. We measure it, derive it (two lines, hinging on "variances of independent things add"), and visualize it from three angles — including the rescaling picture that collapses every snapshot of the distribution onto the same bell curve.

Open [`sqrt_t.ipynb`](sqrt_t.ipynb).
